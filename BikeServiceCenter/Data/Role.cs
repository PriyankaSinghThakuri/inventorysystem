// This enum represents the different roles a user can have
namespace BikeServiceCenter.Data;

public enum Role
{
    // A staff user
    User,

    // An administrator with access to additional features
    Admin
}